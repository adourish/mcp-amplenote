{
  "mcpServers": {
    "amplenote": {
      "type": "stdio",
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/server.js"],
      "env": {
        "AMPLENOTE_CREDS_PATH": "${AMPLENOTE_CREDS_PATH}"
      }
    }
  }
}
